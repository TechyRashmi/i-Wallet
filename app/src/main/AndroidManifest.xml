<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.blucor.i_wallet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:name=".Main.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IWallet">
        <activity android:name="Electricity.ElectricityOperator"></activity>
        <activity android:name="DTH.DTHRechargeActivity" />
        <activity android:name="DTH.DTHActivity"></activity>
        <activity android:name="Recharge.ContactListActivity" />
        <activity android:name=".Main.DashboardActivity" >


        </activity>
        <activity android:name=".Main.OtpActivity" />
        <activity android:name=".Main.LoginActivity" />
        <activity android:name=".Main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>